export default function InstitutionSettingsPage() {
  return (
    <main className="space-y-6 px-4 py-8 md:px-8">
      <h1 className="font-display text-3xl text-[var(--primary)]">Institution Settings</h1>
      <section className="rounded-2xl bg-white p-5">
        <p className="text-sm text-[var(--muted)]">Configure LMS integration, branding, and counsellor workflow.</p>
      </section>
    </main>
  );
}
